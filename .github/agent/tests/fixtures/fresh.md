## Checklist
- [ ] Add a Go doc comment to StockEffect in internal/workflow/spec.go, following the existing doc-comment style in that file. Change nothing else.
- [ ] Add a Go doc comment to DeliveryEffect in internal/workflow/spec.go, following the existing doc-comment style in that file. Change nothing else.
- [ ] Add a Go doc comment to CustomerEffect in internal/workflow/spec.go, following the existing doc-comment style in that file. Change nothing else.
- [ ] Add a Go doc comment to InvoiceEffect in internal/workflow/spec.go, following the existing doc-comment style in that file. Change nothing else.
- [ ] Add a Go doc comment to PurchaseOrderEffect in internal/workflow/spec.go, following the existing doc-comment style in that file. Change nothing else.

<!-- agent-state
turn=0
-->
