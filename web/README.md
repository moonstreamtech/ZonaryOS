# ZonaryOS Frontend

Next.js (App Router) + Tailwind CSS frontend for ZonaryOS. See `../docs/DEVELOPMENT.md` for local setup and `../CLAUDE.md` for working rules.

UI copy is served through `next-intl` from `messages/{locale}.json` — do not hardcode user-facing strings in components.
